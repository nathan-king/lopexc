# `out/`

Purpose: generated compiler outputs (`.dll` files) from local builds/runs.

## Notes

- This folder is for local artifacts, not source.
- Typical files include outputs from:
  - `./lopex build ...`
  - `./lopex run ...`
